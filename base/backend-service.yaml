apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: ai-task-platform
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: ai-task-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: backend
  ports:
    - name: http
      port: 5000
      targetPort: http
