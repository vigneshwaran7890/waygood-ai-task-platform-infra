apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: ai-task-platform
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: ai-task-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: frontend
  ports:
    - name: http
      port: 8080
      targetPort: http
