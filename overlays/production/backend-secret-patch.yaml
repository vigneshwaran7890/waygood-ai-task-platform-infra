# Placeholder values — see docs/SECRETS.md. Production secrets are managed
# with extra care: rotated independently from staging, never reused across
# environments, and applied out-of-band (never committed as real values).
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
stringData:
  JWT_ACCESS_SECRET: "CHANGE_ME_production_access_secret"
  JWT_REFRESH_SECRET: "CHANGE_ME_production_refresh_secret"
